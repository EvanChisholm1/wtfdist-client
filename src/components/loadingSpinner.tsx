import { FC } from "react";

const LoadingSpinner: FC = () => {
  return (
    <div className="animate-spin rounded-full w-10 h-10 border-4 border-indigo-500 border-l-indigo-200"></div>
  );
};

export default LoadingSpinner;
